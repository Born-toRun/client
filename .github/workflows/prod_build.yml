name: Next.js CI with pmpm

on:
  push:
    branches: [ "main", "second-mvp" ] # TODO: prod 준비 되면 main 브랜치로 변경

permissions:
  contents: read

jobs:
  build-docker-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
        with:
          platforms: linux/arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # docker hub login
      - name: docker login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      # docker hub image build and push
      - name: docker image build and push
        run: |
          docker buildx build \
            --platform linux/amd64,linux/arm64 \
            --build-arg NEXT_PUBLIC_BACKEND_URL=https://born-to-run.kro.kr:8443 \
            -t ${{ secrets.DOCKERHUB_USERNAME }}/b2r-client:latest \
            -f docker/Dockerfile \
            --push \
            .

      # discord notify
      - name: discord 알림 전송
        if: success() || failure()
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          DISCORD_EMBEDS: |
            [
                { 
                  "title": "🚀 GitHub Actions 빌드 결과",
                  "color": 10609311,
                  "description": "${{ github.event.issue.html_url }}",
                  "fields": [
                    {
                      "name": "📂 리포지토리",
                      "value": "${{ github.repository }}",
                      "inline": true
                    },
                    {
                      "name": "🌿 브랜치",
                      "value": "${{ github.ref }}",
                      "inline": true
                    },
                    {
                      "name": "✅ 상태",
                      "value": "${{ job.status }}",
                      "inline": true
                    },
                    {
                      "name": "🔗 빌드 로그",
                      "value": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}",
                      "inline": true
                    }
                  ]
                }
              ]
        with:
          args: '${{ github.repository }} GitHub Actions 빌드 완료.'
